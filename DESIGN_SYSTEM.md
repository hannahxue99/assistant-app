# 主人的备忘录设计系统

## 设计方向

克制、温暖、接近苹果备忘录的阅读体验。界面优先保证内容清晰和操作可预期，不用装饰性控件争夺注意力。

## 令牌

- 背景：`#F6F3EE`
- 卡片：`#FFFFFF`
- 正文：`#2B2622`
- 次级文字：`#8A837B`
- 主操作：`#E0733A`
- 置顶强调：`#C99A2E` / `#F8EFD9`
- 间距：4、8、12、16、24、32
- 圆角：输入与小卡片 12，内容卡片 16，胶囊 20
- 字号：页面标题 22，模块标题 17，正文 15，辅助文字 13
- 最小触控区域：44×44

## 组件规则

### 编辑入口

- 浏览态统一显示铅笔，编辑态在同一位置显示“完成”。
- 页面级图标 20px / 文字 15px；模块级图标 18px / 文字 13px。
- 禁用态使用次级文字色，不新增另一套按钮样式。

### 置顶图钉

- 唯一方向：钉帽右上、钉尖左下。
- iOS 使用 `pin` / `pin.fill`，统一旋转 45°；Android/Web 使用 `push_pin` 并由共享组件控制方向。
- 未置顶为灰色线框；已置顶为金色实心，并搭配浅金卡片背景。
- 可见尺寸 19×19，点击区域固定 44×44，点击区域不能撑高内容行。
- 业务页面只能使用 `TopicPinIcon`，不得直接选择其他图标或自行旋转。

## 状态要求

所有可交互组件至少定义：默认、按下、选中、禁用、加载、错误和空状态。没有业务意义的状态可以省略，但必须在设计评审中明确。

### 理解状态

- 正在调用 LLM 且主题尚未生成：在未聚合原文下显示“正在整理…”，不弹窗。
- LLM 失败：显示“原文已保存，暂未整理”和“重试”。
- 最近 10 分钟至少 3 条失败：在聚合列表上方升级显示页面级提示和“检查设置”。
- LLM 成功：记录自然进入聚合卡，不额外显示成功提示。
- 理解引擎关闭或 API Key 未配置：C 端不显示“仅本地规则”状态，保持原有界面。

### 内容时间

- 聚合消息卡与用户原声卡只显示活动时间本身，不加“最新”“最新编辑”或“编辑”常驻前缀。
- 活动时间取该条内容的 `updatedAt`；新建未修改时 `updatedAt = createdAt`。
- 用户原声详情始终显示“创建”；只有标题或正文实际修改后，才增加一行“编辑”。
- 今天/昨天/前天显示相对日期和时刻；本年其他日期显示 `MM-DD HH:mm:ss`；跨年显示 `YYYY-MM-DD HH:mm:ss`。
- 视觉基准见 `design/entry-updated-time.svg`。

## 设计到开发

1. 需求先确定交互和所有状态。
2. 设计稿标注组件名、图标来源、尺寸、颜色和方向。
3. 业务页面复用共享组件，不复制图形或样式。
4. PR 附设计稿和真机截图，逐项核对轮廓、方向、状态、间距和触控区域。
5. 真机与设计稿未对齐前，UI 任务不能标记完成。
