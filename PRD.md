# 个人助手 App — 需求文档 v1.2

> 状态：已与产品 owner 对齐（2026-08-28 初稿；2026-09-01 真机验证后修订）
> 平台：iPhone 优先（Expo / React Native），有 Mac 可本地编译 dev client
> 数据：纯本地 SQLite；理解引擎走 LLM API（自带 key，可一键关闭降级）

## 产品定义

口袋里的外置秘书：**3 秒记下任何事，它比你记得牢、找得到，并逐步学会在对的时刻提醒你。**

## 演进路线（北极星）

| 阶段 | 目标 | 范围 | 进入下一阶段的门 |
|---|---|---|---|
| P1 待办/提醒 | 解决"怕忘事" | **信息理解 + 归档**、今日视图、定时提醒、晨晚提问式通知、语音转文字、全文搜索 | 连续用 2 周 + 50 条以上记录，忘事焦虑明显降低 |
| P2 知识库 | 越来越懂你、快速找到信息 | 标签体系演化、主题聚类、语义搜索、画像自动沉淀（含纠正数据） | 记录 200+，检索成为高频动作 |
| P3 启发 | 主动启发 | 四种触发（时间/状态/关联/画像），LLM 已在架构内 | P1/P2 站稳，且仍觉得缺外部视角 |

## 核心流程：输入 → 理解 → 归档 → 浮现

用户的输入是**口语化**的（"明天下午跟小王碰一下方案的事别忘了"）。原样存储等于死数据，所以 P1 的第一道工序是**信息理解 + 归档**：

1. **原文立即落库**（零摩擦：此刻记录已完成；rawText 保留最初输入，理解可重算）
2. **异步理解（<1s，不阻塞）**，拆解五层：
   - 意图：待办 / 想法 / 信息（LLM）
   - 时间："明天下午" → 具体时刻（规则，本地，可靠）
   - 主题标签：自动 1-3 个标签（LLM）
   - **聚合主题（topic）**：新话题 or 已有主题的更新？理解时携带近 180 天内最近活跃的最多 100 个主题作上下文，LLM 匹配（如「学到第50本绘本」→ 命中「孩子英语学习」）
   - 人物："小王"、"老婆"（P2 启用，做关系维度回顾）

### 状态型信息：原文不合并，视图聚合

信息分两类：**事件型**（"买了牛奶"，办完即结束，时间流并列）与**状态型**（"学到第30本" → "学到第50本"，新记录是旧记录的更新）。

- 数据库层面**永不合并记录**；聚合发生在主题视图层：主题卡 = 最新状态置顶 + 历史轨迹（倒序）
- 主题改名、合并挪至 V1.1（LLM 匹配失误时，V1 先以编辑文案兜底）
- 差值洞察（"一周 +20 本"）留给 P3——主题聚合是它的数据前提
- 实施切分：topic 字段 + 理解时主题匹配 **进 V1**（不做则标签不一致，P2 返工）；主题分组视图是 V1 最后一块，周期紧可挪 V1.1
3. **静默归档**：待办进今日视图、有时间的挂本地提醒、标签落位可检索
4. **在对的时刻浮现**：早晨聚焦页 / 到点提醒 / 搜索命中

### 理解引擎技术决策（已拍板）

- **LLM API 为主**：用户自带 key，OpenAI 兼容协议（DeepSeek/通义/Kimi 任选），成本约几块钱/年
- **规则做时间归一化**：本地、确定性强，不依赖网络
- **降级链**：无网/失败 → 规则结果回填保底、标记 failed，下次启动联网时自动补理解；设置页有**理解总开关**，关闭即纯规则模式
- **Provider 化架构**：理解引擎抽象为可替换接口，未来可平移端侧模型（iOS Foundation Models），业务代码不动

## V1.0 功能清单（P1）

- **记录**：打开即输入框；文字 + 按住说话转文字（系统 STT）；`#标签` 手动补充
- **理解+归档**：输入后自动理解意图/时间/标签/主题并归档；文案可在主题详情页或用户原声详情页**编辑回写**（主题页手动/AI，原声页手动；编辑快照入 correctedFrom，供 P2 画像）
- **待办**：勾选完成（当天划线、次日消失）；理解出的时间自动挂本地提醒，完成/删除自动撤销；逾期待办**不进本周待办**（2026-09-01 改版：本周待办只含今天起 7 天窗口内的未完成项，逾期项经搜索/备忘录可达）
- **首页**：本周待办（今日起向后 7 天，按时间排序，每行 `日期 + 星期 + 时间`；无待办的日期整行不展示）+ 长期待办（7 天之后的未完成待办，按时间升序，行结构同本周待办，无则整模块隐藏；进入 7 天窗口自动升入本周待办）+ 沉底快速记录
- **首页 · 聚合页签**：主题卡（主题名 + 原始信息条数 + 最新一条概要 + 最新时间）与无主题时间流卡混排 + 全文搜索（中文优化，SQLite FTS trigram）；主题可置顶/取消置顶，置顶组按最近置顶时间排列，普通组按最新消息排列；主题卡进主题详情页，时间流卡进用户原声详情页
- **首页 · 用户原声页签**：流水视图，精确到秒的时间戳（createdAt）+ rawText 当前文本；仅做记录展示，不做理解加工
- **通知**：早 8:00 优先提醒今天待办，无则提醒今日起 7 天内待办，再无则提醒最近 5 条未来待办；晚 21:00 优先提醒明日起 7 天内待办，无则提醒最近 5 条未来待办；单次最多展示 5 条，无符合项则不推送。晨晚提醒均可独立开关；待办到点提醒默认开启
- **我的页**：LLM 设置（key/开关）、画像（目标/雷区）、导出 Markdown、通知设置
- **明确不做**：日历视图、习惯打卡、番茄钟、社交、云同步、语音命令（V2 再议）

## 设计原则

1. **零摩擦捕捉**：任何场景 3 秒内记完一条
2. **记录永不阻塞**：理解是增强不是门槛，理解全挂也是个能用的备忘录
3. **文案可编辑**（2026-09-01 修订）：summary 与 rawText 均可在用户原声详情页编辑；编辑前快照存入 correctedFrom（含改前 rawText，仅存档不展示），最初输入在库中可追溯；标题=原文的条目编辑时保持同文
4. **编辑即学习**：每次手动/AI 编辑的快照入库（correctedFrom，不展示历史），是 P2 画像最早的真实数据
5. **克制 + 有重点**：晨晚推送每日最多 2 条，每条最多汇总 5 项；优先展示临近待办
6. **轻结构**：不强迫分类，标签自动+随手打，结构自然生长
7. **数据可检索优先**：从第一天按知识库标准建模，P2 不返工

## 页面结构

2 个底 Tab：`首页`（本周待办 + 聚合/用户原声双页签 + 沉底快速记录）｜`我的`（LLM 设置+画像+导出+通知）

- **首页**：无页面大标题，第一视觉为「本周待办」主模块，下方「长期待办」次级模块；再下方「主人的备忘录」模块（聚合 / 用户原声双页签）；Composer 沉底固定，输入框带边框，语音用图标
- **详情页**（Tab 外 stack 页，两种）：用户原声详情页（时间戳 + 标题 + 原文，均可编辑 + 删除）｜主题详情页（#主题可改名 + 条数 + 消息卡片流；改成已有主题时确认后合并）
- **设置子页**（Tab 外 stack 页）：仅理解引擎进子页；提醒行内开关、导出直接调系统分享面板

## 数据模型（为知识库设计）

```
Entry  { id, rawText,                 -- 原文（用户可编辑；改前快照存 correctedFrom 可追溯）
         kind: task|idea|info,        -- 理解结果：意图
         summary,                     -- 当前文案（理解生成或手动编辑，聚合展示，不存历史版本）
         dueAt?, remindAt?,           -- 提取/设定的时间
         topic?,                      -- 聚合主题；LLM 未成功理解时为空，可改名/合并
         tags[], persons[],           -- 主题标签、人物（P2 启用）
         parseStatus: pending|ok|failed|manual,
         parseSource: rule|llm|manual,
         correctedFrom?,              -- 编辑前后快照（不展示历史，供 P2 画像）
         createdAt, done, doneAt, source: text|voice }
Profile{ name, goals[], avoid[], notifyMorning, notifyEvening }
Settings{ llmKey, llmBaseUrl, llmModel, llmEnabled }
```

- SQLite 持久化；FTS5 trigram 虚拟表支撑中文全文搜索（索引 summary + rawText + tags）
- 全部数据可导出为 Markdown（供 AI 深度分析的离线闭环）
