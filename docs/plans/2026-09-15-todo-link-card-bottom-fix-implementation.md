# 待办跳链与事件卡底部留白实施计划

**目标：** 修复小知跳链参数重复覆盖手动 Tab 的问题，并消除事件卡固定高度形成的底部空白。

## Step 1：把跳链建模为一次性意图

- 输出：`src/assistant/ui-state.ts` 提供标准化意图和稳定 key；首页在更新 Tab 前判断该意图是否已消费。
- 测试：同一参数第二次返回 `isNew=false`；不同回执仍返回新的正确目标。

## Step 2：让事件卡由内容自然撑高

- 输出：移除卡片与内容层 `minHeight`，保留三行及 44px 置顶热区，统一上下内边距和行间距。
- 测试：TypeScript 与现有 UI 测试通过；Dev 真机检查底部无明显留白。

## Step 3：完整验证与交付

- 输出：更新设计文档，运行 `npm run ci`，提交并推送到现有 PR #19，使用已有 Metro 会话 Reload。
- 测试：CI 全绿；真机可从全部待办切回本周，事件卡视觉紧凑。
